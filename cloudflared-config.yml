tunnel: bd42e2a6-0d91-4761-89af-f7e8b302f3a3
credentials-file: /etc/cloudflared/credentials.json

ingress:
  - hostname: tailscale-httpbin-docker.dhrunners.dpdns.org
    service: http://localhost:8090
  # SSH over Cloudflare Tunnel (requires DNS record + Cloudflare Access policy).
  - hostname: ssh-tailscale-httpbin-docker.dhrunners.dpdns.org
    service: ssh://localhost:22
  - service: http_status:404
